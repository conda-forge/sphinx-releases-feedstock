{% set name = "sphinx-releases" %}
{% set pypi_name = "releases" %}
{% set version = "2.0.0" %}
{% set sha256 = "99296a3acab3838e27e96ec5c3713450982f385b6b6fd54fc9e2425393972e8c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ pypi_name[0] }}/{{ pypi_name }}/{{ pypi_name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - semantic_version <2.7
    - sphinx >=1.8

test:
  imports:
    - releases

about:
  home: https://github.com/bitprophet/releases
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE
  summary: A powerful Sphinx changelog-generating extension
  doc_url: http://releases.readthedocs.io/
  dev_url: https://github.com/bitprophet/releases

extra:
  recipe-maintainers:
    - kwilcox
